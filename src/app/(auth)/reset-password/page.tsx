import { ResetPasswordForm } from "@/components/password-reset-forms";

export const metadata = {
  title: "Reset Password | LaunchRadar",
};

export default function ResetPasswordPage() {
  return <ResetPasswordForm />;
}
